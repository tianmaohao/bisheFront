// 应用设置
export default {
  // 侧边栏配置
  sidebar: {
    width: 200,
    collapsedWidth: 64,
    isCollapse: false
  },
  
  // 头部配置
  header: {
    height: 60
  },
  
  // 标签页配置
  tagsView: {
    enabled: true,
    maxCount: 10
  },
  
  // 主题配置
  theme: {
    primaryColor: '#409EFF',
    successColor: '#67C23A',
    warningColor: '#E6A23C',
    dangerColor: '#F56C6C',
    infoColor: '#909399'
  },
  
  // 表格配置
  table: {
    stripe: true,
    border: true,
    size: 'default'
  }
}

